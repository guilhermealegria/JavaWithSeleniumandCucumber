pipeline {
    agent any
    tools{
        maven 'Maven 3.8.6'
        jdk 'jdk14'
    }
    stages {
        stage('Initialize') {
            steps {
                sh '''
                    echo "PATH = ${PATH}"
                    echo "M2 = ${M2}"
                '''
            }
        }
        stage('Build') {
            steps {
                    echo 'Compilando..'
            }
        }
    }
}