pipeline {
    agent any 
    tools{
         maven 'M3'
        }
    stages {
        stage('Initialize') {
            steps {
                sh '''
                    echo "M3 = $PATH{M3}"
                '''
            }
        }
    }
}
