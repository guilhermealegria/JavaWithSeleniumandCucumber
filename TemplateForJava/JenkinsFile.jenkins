pipeline {
    agent any 
    tools{
         maven 'M3'
        }
    stages {
        stage('Initialize') {
            steps {
                bat '''
                    echo "M3 = $PATH{M3}"
                    echo "WORKSPACE = ${WORKSPACE}"
                '''
            }
        }
         stage('Build') {
            environment{
                    WORKSPACE = '/var/jenkins_home/workspace/TestJenkinsFile/TemplateForJava'
            }
            steps {
                bat '${WORKSPACE} mvn -B -DskipTests clean package'
            }
        }
    }
}
